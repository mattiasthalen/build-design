# build-design

Claude Code plugin: turns a design brief produced by the `design` plugin into a plan approved in plan mode and built by a workflow, one commit per unit.

Design documentation: decisions in `docs/adr/`, vocabulary in `docs/lexicon.md`, standing assumptions in `docs/assumptions.md`. Feature briefs live on GitHub issues, not in the tree.
